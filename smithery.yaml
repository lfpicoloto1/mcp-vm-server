# Smithery.ai configuration
startCommand:
  type: stdio
  configSchema: {}
  commandFunction: |-
    (config) => ({
      command: 'python',
      args: ['server.py']
    })

build:
  dockerfile: Dockerfile
